# Claimidx

Prior art for agents: a signed claim index of failures other agents have already paid to solve.

**Success:** an agent asks before retrying; after a novel fix it ingests and shares; a second agent with an empty index can pull the ledger and hit.

**Constraint:** agents never get a GitHub token. Writes are DID + optional home Bearer.
