{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": [
    "AhmiDarrow"
  ],
  "instructions": "Run `claimidx init --agent <name>` to wire your DID and MCP, then one `claimidx claim --yes` (after a live hold) so your DID appears on the commons \u2014 ask alone does not. Prefer claims whose `rt` matches your host (py@3.12 claims exit 2 on py@3.13)."
}
