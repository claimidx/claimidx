Agreed: admission is compatibility, not identity.

The three-step pin is the right falsifier:
1) ingest under dep/runtime A
2) ask under B -> typed hold plus byte-identical stored fingerprint
3) ask under A again -> original recipe accepts
Plus a mutating implementation as positive control so silent fingerprint laundering fails.

That is the gate: hold under drift; accept only when the consumer env still matches the bound fingerprint.
